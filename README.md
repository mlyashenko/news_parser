# 📰 News Parser

**News Parser** — учебный проект на Python для парсинга новостной HTML‑страницы  
и преобразования новостей в структурированный формат JSON.

---

## 📌 Описание проекта
Проект извлекает новости из сохранённой HTML‑страницы (WebArchive → iz.ru/news),  
определяет рубрику (Общество, Армия, Здоровье и т.д.), заголовок, ссылку, описание  
и время публикации.

Все данные группируются в словарь Python и сохраняются в `news_parsed.json`.

---

## 📁 Структура проекта
```
news_parser/
├── parse_news.py        # основной парсер
├── requirements.txt     # зависимости (beautifulsoup4, lxml)
├── .gitignore           # исключения: venv, pycache, системные файлы
└── README.md            # описание проекта
```

---

## ⚙️ Как работает парсер
1. Загружает файл `news.html`.
2. Находит блоки новостей с помощью BeautifulSoup.
3. Извлекает:
   - рубрику,
   - заголовок,
   - описание,
   - ссылку,
   - время публикации.
4. Формирует итоговую JSON‑структуру.
5. Записывает результат в `news_parsed.json`.

---

## 🛠 Установка и запуск

### 1️⃣ Клонирование проекта
```bash
git clone https://github.com/USERNAME/news_parser.git
cd news_parser
```

### 2️⃣ Создание виртуального окружения
```bash
python3 -m venv news_venv
```

### 3️⃣ Активация окружения
**macOS / Linux:**
```bash
source news_venv/bin/activate
```
**Windows:**
```bash
news_venv\Scripts\activate
```

### 4️⃣ Установка зависимостей
```bash
pip install -r requirements.txt
```

### 5️⃣ Подготовка HTML
Скачать страницу из WebArchive и сохранить как `news.html` в корень проекта.

### 6️⃣ Запуск
```bash
python parse_news.py
```

---

## 📦 Используемые технологии
- Python 3.9+
- BeautifulSoup4
- lxml
- Git / GitHub
- Virtualenv

---

## 👨‍💻 Автор
Учебный проект по теме «Парсинг веб‑страниц в Python».

